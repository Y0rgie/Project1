#include<stdio.h> //standard library

int encrypt(void); //function header  - this is currently doing nothing
int letter(int *A);


int main() { //code block start//
printf("Insert a body of text: \n");


// TO convert letters to ASCII
    int A; //initialise the pointer value, where A is each letter of the text
    int B; // B is the "real" number of letter which is convertible
    for (A=0; A<N; A++){
        if (c>=65 && c<=90){ //case of CAPITAL in ASCII
        B = c - 64;
        //store B
        }
        if (c>=97 && c<=122){
        B = c - 96;
        //store B
        }
    }
    
    char c;
    scanf("%c", &c); //converts explicitly to ASCII - but only ffor each letter...
    
    

//input key
//input of text with spaces and punctuation accpeted

//run function

//print encrypted text
return 0;
}

//define function algorithm